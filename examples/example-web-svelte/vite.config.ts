import { svelte } from '@sveltejs/vite-plugin-svelte'
import { defineConfig } from 'vite'

// Vite configuration for the example-web-svelte app
export default defineConfig({
  plugins: [svelte()],
})

