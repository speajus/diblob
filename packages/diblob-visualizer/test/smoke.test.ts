import { test } from 'node:test';
import assert from 'node:assert/strict';

// Simple smoke test so the package has at least one node:test-based spec
test('diblob-visualizer smoke test runs', () => {
  assert.equal(1 + 1, 2);
});

